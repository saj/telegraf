FROM debian:stretch

RUN \
  apt-get update \
  && apt-get install -y ca-certificates \
  && rm -rf /var/lib/apt/lists/*

ARG package
ADD ${package} ${package}
RUN dpkg -i ${package}

EXPOSE 8125/udp 8092/udp 8094

CMD ["telegraf"]
